---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bootinghok                  # Ingress name
  labels:
    app: bootinghok
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "bootinghok.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bootinghok               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
